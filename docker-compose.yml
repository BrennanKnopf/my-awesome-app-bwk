version: '3.9'

services:

  my-awesome-app-bwk:
    container_name: my-awesome-app-bwk
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
     - "8080:80"
    volumes:
      - '/app/node_modules'
      - .:/app